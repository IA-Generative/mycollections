apiVersion: v1
kind: Service
metadata:
  name: myrag-frontend
  namespace: miraiku
spec:
  selector:
    app: myrag-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
